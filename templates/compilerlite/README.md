# compilerlite (arena README)

Goal: implement a tiny programming language with a lexer+parser, bytecode compiler, VM, and CLI.

Run tests:

```bash
python -m pytest -q target/tests
python -m pytest -q target/tests_hidden
```

