# snakelite (arena README)

Goal: implement a small, replayable Snake game. Tests use a replay JSON file and validate game logic and outputs.

Run tests:

```bash
python -m pytest -q target/tests
```

